<%= form_with(model: shop, data: { turbo: false }, local: true) do |form| %>
  <% if shop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shop.errors.count, "error") %> prohibited this shop from being saved:</h2>
      <ul>
        <% shop.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <form>
    <div class="mb-3">
      <%= form.label '店舗名' %>
      <%= form.text_field :name, class: "form-control" %>
    </div>
    <div class="mb-3">
      <%= form.label 'google口コミリンク' %>
      <%= form.text_field :url, class: "form-control"%>
    </div>
    <div class="mb-3">
      <%= form.label 'TripAdvisor口コミリンク' %>
      <%= form.text_field :url_tripadvisor, class: "form-control"%>
    </div>
    <%= form.submit shop.new_record? ? '作成' : '更新', class: "btn btn-success" %>
    </form>
<% end %>