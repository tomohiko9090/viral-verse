<div class="container">
  <div class="row justify-content-center">
    <div class="col-lg-8">
      <div class="card shadow-sm">
        <div class="card-body p-4">
          <h2 class="h3 mb-3 text-center">サービス改善のご提案 (2/2)</h2>

          <p class="text-muted text-center mb-4">
            もう一点だけ、ご意見をお聞かせください。
          </p>

          <%= form_with(model: @review,
                        url: submit_survey2_shop_review_path(@shop, @review),
                        data: { turbo: false },
                        method: :post
                        ) do |form| %>
            <div class="mb-4">
              <label class="form-label">このようなサービスがあったら良かったなと思うことはありますか？</label>
              <%= form.text_area :feedback2,
                  class: "form-control",
                  rows: 4,
                  placeholder: "例：予約システム、メニューの種類、設備など",
                  required: true %>
            </div>

            <div class="text-center">
              <%= form.submit '完了', class: 'btn btn-primary btn-lg px-5' %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>